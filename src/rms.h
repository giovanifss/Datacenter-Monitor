#ifndef RATE_MONOTONIC_SCHEDULER_H
#define RATE_MONOTONIC_SCHEDULER_H

#include"task.h"

/*
 * Assigns a priority to task based on the period
 * The shorter the period, the higher the priority
 */
// Define priority

/*
 * Initiates the scheduing of the tasks
 */
// Start scheduling

/*
 * Stops the scheduling of the tasks
 */
// Stop scheduling

#endif /* RATE_MONOTONIC_SCHEDULER_H */
